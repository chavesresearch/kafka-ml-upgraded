litestar==2.14.0
sqlalchemy==2.0.36
greenlet==3.1.1
aiosqlite==0.20.0
alembic==1.14.0
aiokafka==0.12.0
kubernetes-asyncio==32.0.0
httpx==0.28.1
paho-mqtt==2.1.0
# Pinned to match the original Django backend's blockchain feature. This
# forces websockets<10 (see below) - web3 7.x drops that constraint but
# changes a lot of the sync w3.eth.* API used in app/blockchain.py, so
# upgrading it is a separate, deliberate decision, not a side effect of
# this port.
web3==5.28.0
py-solc-x==2.0.2
loguru==0.7.3
msgspec==0.18.6
# Plain uvicorn + wsproto (not uvicorn[standard], which pulls in
# websockets>=10.4 and conflicts with web3's websockets<10 pin) - wsproto is
# uvicorn's other supported ASGI websocket implementation.
uvicorn==0.34.0
wsproto==1.2.0
httptools==0.6.4
python-multipart==0.0.20
