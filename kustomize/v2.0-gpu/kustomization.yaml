apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - "../v2.0"

components:
  - "../components/gpu-executor-images"

configMapGenerator:
  - name: kafkaml-configmap
    behavior: merge
    literals:
      - tensorflow.training.image=ertis/kafka-ml-tensorflow_model_training-gpu:v2.0
      - tensorflow.inference.image=ertis/kafka-ml-tensorflow_model_inference-gpu:v2.0
      - pytorch.training.image=ertis/kafka-ml-pytorch_model_training-gpu:v2.0
      - pytorch.inference.image=ertis/kafka-ml-pytorch_model_inference-gpu:v2.0
