on:
  push:
    paths:
      - model_inference/tensorflow/**/*
    branches:
      - "master"
      - "main"
      - "latest"
      - "dev*"
  pull_request:
    paths:
      - model_inference/tensorflow/**/*
  release:
    types:
      - created
jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: model_inference/tensorflow
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "0.9.x"
      - name: Run tests
        run: uv run pytest -v

  build-tf-model-inference:
    needs: test
    if: github.event_name != 'pull_request'
    strategy:
      matrix:
        include:
          - name: kafka-ml-tensorflow_model_inference
          - name: kafka-ml-tensorflow_model_inference-gpu
            build-args: "TFTAG=2.21.0-gpu"
    uses: ./.github/workflows/build.yml
    with:
      context: model_inference/tensorflow
      dockerfile: model_inference/tensorflow/Dockerfile
      name: ${{ matrix.name }}
      platforms: linux/amd64
      build-args: ${{ matrix.build-args }}
    secrets: inherit
